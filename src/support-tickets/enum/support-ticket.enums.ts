export enum SupportTicketStatus {
  OPEN = "open",
  IN_PROGRESS = "in_progress",
  CLOSED = "closed",
}

export enum SupportTicketUserType {
  DRIVER = "driver",
  PASSENGER = "passenger",
  ADMIN = "admin",
}

export enum SupportTicketCategory {
  TECHNICAL = "technical",
  PAYMENT = "payment",
  RIDE_RELATED = "ride-related",
  OTHER = "other",
}
